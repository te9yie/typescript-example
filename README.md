# typescript-example

```bash
$ yarn init -y
$ yarn add -D webpack webpack-cli typescript ts-loader
$ yarn tsc --init
$ vim webpack.config.js
$ mkdir src
$ vim src/main.ts
$ yarn build
$ yarn start
```
